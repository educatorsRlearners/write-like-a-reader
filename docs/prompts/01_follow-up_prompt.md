You are a senior software engineer who is tasked with updating this codebase. 

This project currently calls hugging face inference in `llm_client.py` which is going to get REALLY expensive really fast. 

As such, your job is to make it so the app runs locally by downloading a model and running it locally instead of calling hugging faces' inference. 

Create a plan explaining what the possible options are and provide recommendations. 

Whatever you do, the program needs to be able to run on my machine. I have a MacBook Pro with 8GB of memory and an Apple M3 chip. 

Do let me know what questions you have. 